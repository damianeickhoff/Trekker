# npm 10's resolver crashes ("reading 'edgesOut'") while walking vitest's
# optional peer set. Skipping peer auto-install sidesteps it; every peer that
# matters here is a direct dependency anyway.
legacy-peer-deps=true
