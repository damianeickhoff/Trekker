import { SettingsBones } from "@/components/settings/bones";

/** Settings with the Badges entry current and the take-back's rows. */
export default function AdminBadgesLoading() {
  return <SettingsBones current={5} rows={5} />;
}
